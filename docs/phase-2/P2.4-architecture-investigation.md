# P2.4 — Technical Architecture Investigation

**This is an investigation, not a decision.** It surfaces and compares viable architecture approaches for the product defined by the Blueprint and the accepted P1.1–P2.3 documents. It does **not** select the final architecture (that is P2.5) and prescribes no technology except where the analysis shows a class of technology is required by a stated requirement.

Inputs read: `Claude_Project_Blueprint.md`, `docs/PROGRESS.md`, `docs/phase-1/P1.1`, `docs/phase-1/P1.2`, `docs/phase-2/P2.1`, `docs/phase-2/P2.2`, `docs/phase-2/P2.3`. Repository state: documentation-only; no application code, no build config, no CI; git with `main` (stable) and `develop`.

---

## 1. Objective

Determine which architecture *shape* best fits the actual, documented requirements of the Personal Developer Workspace, its Context Engine, and its AI Assistant — evaluating at least three independent approaches across every dimension listed in the P2.4 brief — and identify the strongest candidate plus the decisions P2.5 must still make. Every recommendation traces to a numbered requirement (Rn) or constraint (Cn) below.

---

## 2. Architectural Requirements

Extracted from the accepted documents. These are things the architecture **must** support.

| # | Requirement | Source |
|---|---|---|
| R1 | Seven object types (Project, Task, Note, Idea, Decision, Resource, Checkpoint) plus a **derived** Work Context view assembled, not stored. | P2.1 §2, §6 |
| R2 | Typed, directional relationships over a fixed vocabulary (10 verbs). `belongs to` is singular; the rest many-to-many. Every relationship records author + timestamp. | P2.1 §8 |
| R3 | Explicit / user-confirmed relationships are **authoritative and immediately honored**, stored distinctly from inferred ones, and are never silently removed. | P2.2 §1, §4, §5 |
| R4 | Inferred relationships carry one of four confidence states (Known-structural, User-confirmed, Inferred-high, Weak) with **hybrid persistence**: candidates recomputed as inputs change; user accept/reject and usage feedback persisted and always reapplied (suppression survives recompute). | P2.2 §4, §5 |
| R5 | Context Engine consumes five signal categories: explicit relationships, structural facts (shared Project, Checkpoint anchoring, `supersedes` chains, Idea→Task lineage, owner, Task state), temporal proximity within a bounded "work session", content/semantic similarity + a workspace-internal distinctive-term/entity index, and per-user usage signals. | P2.2 §2 |
| R6 | Purpose-scoped **Context Set** assembly (Resume / Decision-recall / Handoff) that is layered (Direct, Indirect, Recent, Historical, High-confidence, Potential), bounded by per-layer caps, deduplicated to the strongest path, and ranked. | P2.2 §6 |
| R7 | Ranking is **reproducible** given the same workspace state + persisted user decisions + engine inputs, and its contributing factors are **explainable**. The inference mechanism itself may be non-deterministic. | P2.2 §7 (as corrected) |
| R8 | Provenance is first-class: every relationship carries its author or the signals that produced it; every object carries creator + contributors; Task/Project carry an owner. | P2.1 §2, §8; P2.2 §6 |
| R9 | Privacy: private-by-default; explicit **Project-level** sharing (MVP); the engine computes context **per requesting user** and filters before context leaves it; a cross-Project relationship is shown only if the viewer can see **both** endpoints; rankings/counts must not leak the existence of invisible objects (no side channel). | P2.1 §4; P2.2 §11; P2.3 §11 |
| R9a | **Relationship-level visibility.** Endpoint visibility is necessary but *not sufficient*: a teammate viewing a shared Project sees shared-scope objects "plus **each viewer's own private links** to those objects" — so a relationship authored privately between two shared objects must be visible only to its author. Relationship visibility is therefore its own attribute, not a pure function of its endpoints. | P2.2 §11 |
| R10 | Handoff: transfer the owner of a Task/Project; deliver an assembled, recipient-visibility-filtered Context Set + a handoff note; a Handoff Inbox; an engine completeness check; a "additional private context exists" flag to the sender. | P2.1 §4; P2.2 §6, §10; P2.3 §3 |
| R11 | The AI Assistant is a **separate component** with a one-directional dependency on the Context Engine. The Engine must deliver value with no Assistant present. | P2.2 §9; P2.3 §2 |
| R12 | The Assistant holds **no** authoritative relationships; any connection it notices enters only through the engine's human-in-the-loop suggestion path. Its output is untrusted data, never an instruction. | P2.2 §9; P2.3 §2, §9, §15 |
| R13 | Every workspace mutation requires explicit user confirmation. **Zero automatic mutations in the MVP.** Confirmed writes are attributed to the user (assistant-assisted). | P2.3 §9, §13 |
| R14 | Conversation state is separate from workspace memory; nothing in a conversation becomes workspace context except via a confirmed action. | P2.3 §12 |
| R15 | Auditability: every assistant-proposed and user-confirmed mutation is recorded (who, when, originating conversation, supporting context). | P2.3 §15 |
| R16 | Graceful context-retrieval failure: if the engine call fails/returns nothing, the Assistant reports the failure and offers retry — it must not substitute model priors or history. | P2.3 §6 |
| R17 | Individual-first: fully functional for a solo developer with **no** team features present; team support is a thin additive layer on the same model. | P1.2 §6; P2.1 §4 |
| R18 | Staleness is surfaced (Checkpoint recency) and time-sensitive signals decay; assembled context must expose "last updated N ago". | P2.2 §5; P2.1 §6 |
| R19 | Phase-4 external integrations (GitHub issues/PRs/commits, chat) must be **addable later** as new object subtypes and new signal sources without reworking the core. | Blueprint Phase 4 (roadmap §2); P2.2 §12 |
| R20 | Visible vertical-slice delivery with a workable local development experience is a project value; large invisible infrastructure ahead of usable product is explicitly discouraged. | Blueprint §5; dev-approach section |
| R21 | **Unfiled and mutable captures.** A capture may exist with **no home Project** (Inbox) until filed, and its **type is changeable after creation** ("defaults to Note"; "refinement (retype, move, link) happens later without penalty"). *Implication:* `home_project` is optional, and object type is mutable — a physical model that binds type to a separate detail table must make re-typing a supported, cheap transaction, not a migration. | P2.1 §3, §5, §7, §9 |
| R22 | **Per-object activity history in the MVP:** checkpoints and edits over time as a flat activity list on the Task view (timeline browsing deferred). Deleting an object removes its relationships while their prior existence may remain in history. | P2.1 §6, §9; P2.2 §5 |
| R23 | **Evaluation telemetry is a product requirement, not an afterthought.** Both accepted designs define what must eventually be measured — suggestion accept/reject and correction rates, false-positive/false-negative signals, manual-linking trend, Context Set churn, ranking position of the first-acted item, handoff follow-ups, answer groundedness and unsupported-claim rate, and a zero-incident privacy invariant. The architecture must be able to capture these signals **without** itself violating R9. | P2.2 §13; P2.3 §14, §16 |

---

## 3. Architectural Constraints

Non-negotiable shape constraints (stronger than requirements — they rule options in or out).

| # | Constraint |
|---|---|
| C1 | **Component boundary (logical, not a deployment topology):** Context Engine and AI Assistant are distinct components with a one-way dependency (Assistant → Engine); the Engine must deliver value with no Assistant present. What the accepted documents actually mandate is the *responsibility split and the call surface* — the Assistant may only (i) read purpose-scoped, already-filtered Context Sets and (ii) post to the HITL suggestion queue. **Whether the Assistant is a separate process, container, or security zone is an open decision, not a constraint** (P2.3 §16 "Assistant trust zone: same network/security zone as the Engine or isolated"; carried here as Q10). Process separation is one way to enforce C4/R12, not the requirement itself. |
| C2 | **Store separation:** the authoritative relationship store and the inferred/candidate store have different lifecycles and MUST be separable (independent recompute, suppression, and retention of the candidate side without touching authoritative rows). |
| C3 | **Privacy enforced in the Engine, centrally:** visibility filtering happens before any context leaves the Context Engine; permission logic is not in the Assistant; it should be enforced at as few code paths as possible to be auditable. |
| C4 | **Confirmed-write-only path:** all mutations flow through one explicit user-confirmed path; assistant/model output cannot trigger a write, a permission change, or an engine-config change on its own. |
| C5 | **Provenance/authorship are structural**, present on every relationship and object from the first schema — not a later addition. |
| C6 | **Reproducible + explainable ranking:** given identical inputs the ranked Context Set is identical, and the engine can state which factors placed each item. Ranking weights must therefore be explicit and versioned. |
| C7 | **No prescribed technology from prior docs:** the open-decision lists in P2.2 §16 and P2.3 §16 (graph representation, embeddings, retrieval mechanism, event-vs-batch inference, recalculation frequency, model selection, storage, conversation persistence) remain open here. |
| C8 | **Phase-4 additivity:** external activity is ingested as more objects/relationships/signals into the same model; the core must not assume the workspace is the only data source. |
| C9 | **Solo has no hard multi-tenant dependency:** single-user operation must not require a running sharing/permission service or team infrastructure. |

---

## 4. Assumptions

Explicitly labelled; each is an input P2.5 should confirm. Where an assumption materially changes the recommendation it is noted.

| # | Assumption | If wrong |
|---|---|---|
| A1 | **Scale:** individual developers and teams up to ~10. Workspace size grows to ~10⁴–10⁵ objects per workspace over years, not 10⁶+. Read/assembly is the hot path; writes are human-rate (low). | **Less decision-changing than first assessed.** Context Set assembly is a *bounded-neighborhood* query anchored on one object — hop-capped at 2 with per-layer caps (R6) — so its cost scales with the anchor's local degree and the per-user visible scope, **not** with total object count; an indexed edge table serves it at 10⁶ objects much as at 10⁴. What genuinely scales with corpus size is the similarity index (already behind the retrieval seam) and signal **recomputation**, which is batch work that can be isolated to workers/replicas *inside* Alt A. The real trigger for Alt C's shape is therefore not object count but: several concerns needing **independent scaling simultaneously**, plus a team large enough to operate them. Q1 is consequently an assumption to **validate**, not a gate that flips the recommendation. |
| A2 | **Deployment:** a hosted service is the primary delivery, with a credible self-host path valued for data ownership. Tenancy model (single vs multi) is undecided. | If self-host is a v1 hard requirement, operational simplicity (Alt A) weighs more; if large multi-tenant SaaS only, Alt C's isolation weighs more. |
| A3 | **Latency:** Resume Context Set assembly should feel interactive (~≤1 s p95); Assistant answers may stream over seconds. | Tighter targets favor precomputed/denormalized assembly. |
| A4 | **Semantic signal needs embeddings:** the content/semantic signal in R5 realistically requires vector embeddings of object text; the technique/provider/hosting is open (P2.2 §16). A distinctive-term/entity index and full-text search are also needed regardless. | If embeddings are deferred out of MVP, all three alternatives simplify equally; the comparison is unchanged. |
| A5 | The "work session" temporal window is a bounded, configurable interval (exact value open, P2.2 §16). | — |
| A6 | Sharing stays **Project-level** for the MVP (object-level deferred, P2.1 §9). | Object-level sharing raises the cost of privacy filtering in every alternative, most in Alt B/C. |
| A7 | Developer content may be proprietary/sensitive → encryption at rest is expected and external-AI egress of raw content is a privacy concern; no specific compliance regime (SOC2/GDPR/HIPAA) is assumed yet. | A named regime raises audit/isolation requirements across the board. |
| A8 | Ranking factor weights are fixed constants in the MVP (no learned weighting, P2.2 §12), versioned for reproducibility (C6). | Learned weighting later needs a feature store / training path (additive in all alternatives). |

---

## 5. Evaluation Criteria

Weighted by how directly each serves a documented requirement. **H/M/L** = weight.

| Criterion | Weight | Tied to |
|---|---|---|
| EC1 Context Engine conceptual fit (relationships + purpose-scoped assembly) | H | R2, R5, R6 |
| EC2 Privacy enforceability — can visibility + no-side-channel be centralised and audited | H | R9, C3 |
| EC3 Authoritative vs inferred separation | H | R3, R4, C2 |
| EC4 Reproducible + explainable ranking | H | R7, C6 |
| EC5 Provenance + unified audit | H | R8, R15, C5 |
| EC6 Individual-first simplicity (solo works with nothing else running) | H | R17, C9 |
| EC7 Local dev experience + time-to-first-vertical-slice | H | R20 |
| EC8 Development velocity (ongoing) | M | R20 |
| EC9 Phase-4 extensibility | M | R19, C8 |
| EC10 Scalability headroom **relative to A1 need** | M | A1 |
| EC11 Hot-path performance for MVP assembly | M | A3, R6 |
| EC12 Operational complexity (lower is better) | H | R20, A2 |
| EC13 Cost (lower is better at target scale) | M | — |
| EC14 Data portability / export (canonical dump) | H | **A2 only — see sourcing note** |
| EC15 System portability / self-host feasibility | M | **A2 only — see sourcing note** |
| EC16 Testing tractability (privacy tests, ranking determinism, assembly) | H | R7, R9 |
| EC17 Migration flexibility later (can evolve toward another shape) | M | — |
| EC18 Alignment with "avoid unnecessary complexity" | H | Project operating instructions (TECHNOLOGY: "avoid unnecessary complexity"); Blueprint §5 |
| EC19 Evaluation-telemetry capture + isolation of background inference work | M | R23, and P2.2 §16 / P2.3 §16 (inference scheduling, evaluation infrastructure) |

**Sourcing note (material to the matrix).** EC14 and EC15 are **not traceable to any accepted requirement**: the words *portability*, *export*, *data ownership*, and *self-host* appear nowhere in `Claude_Project_Blueprint.md`, P1.1, P1.2, P2.1, P2.2, or P2.3. They derive solely from **assumption A2**, introduced by this investigation. Earlier drafts attributed them to "Blueprint §4" — that attribution is withdrawn. They are retained as criteria because a self-host/export path is a plausible product goal for a developer-facing context tool, but their weight is **assumption-derived and must be confirmed in P2.5**; §11 records the sensitivity of the result to dropping them. EC18's substance is real but comes from the project operating instructions, not the Blueprint.

---

## 6. Context Engine — architectural implications (called out per the P2.4 brief)

The architecture must preserve, regardless of alternative:

- **Explicit relationships** — a first-class, authoritative, immediately-consistent store, physically/logically separate from inferred candidates (R3, C2). *Implication:* the SoT for relationships cannot be a lossy projection or a similarity index; it is a transactional record.
- **Purpose-scoped Context Sets** — assembly is a parametrised operation over the relationship + object stores plus the signal indexes, producing a bounded, ranked, layered result (R6). *Implication:* the engine needs cheap structural traversal to ~2 hops, a similarity lookup, a temporal filter, and an app-controlled ranker; it does **not** need arbitrary-depth graph analytics in the MVP (P2.2 §12).
- **Provenance** — carried on every edge/object row and returned in the Context Set (R8, C5). *Implication:* schema-level, not a log-only afterthought.
- **Privacy boundaries** — one enforcement point inside the engine, applied before emission; both-endpoints-visible for cross-Project links **and** the relationship's own visibility (R9a — a viewer's private link between two shared objects is theirs alone); no counts/rankings computed over rows the viewer can't see (R9, C3). *Implication:* prefer an architecture where the visibility predicate lives in **one** query path, and where that predicate can express relationship-level as well as endpoint-level scope.
- **User authority + confirm-before-write** — inferred links never auto-promote; writes are one confirmed path (R13, C4). *Implication:* the candidate store is append/adjust-only from the engine; promotion to authoritative is a user-triggered transaction.
- **Assistant-as-untrusted output** — the engine exposes a read API returning structured, already-filtered context; it accepts *suggestions* from the Assistant only through the same HITL queue a human suggestion uses (R12). *Implication:* the engine's write surface for the Assistant is exactly the suggestion queue, nothing more.
- **Graceful context-retrieval failure** — the engine API distinguishes "empty result" from "unavailable"; the Assistant gateway maps unavailability to a retry-offer, never a fallback (R16). *Implication:* a typed error contract on the engine API.

These do not by themselves pick an alternative, but they penalise any option that spreads the visibility predicate across many code paths (EC2) or blurs the authoritative/candidate boundary (EC3).

---

## 7. Architecture Alternative A — Relational-core modular monolith with pluggable retrieval

**Shape.** One deployable application ("workspace + engine") with enforced internal module boundaries (Workspace, Context Engine, Identity/Sharing), plus a **separate** Assistant service (the untrusted boundary, C1/R11). A single relational database is the system of record. Content search and vector similarity are provided by the relational engine's own capabilities or first-class extensions, behind a `RetrievalProvider` interface so they can move out later.

| Dimension | Analysis |
|---|---|
| Application architecture | Modular monolith; Context Engine exposed via an in-process API with the *same* contract it would have as a service (typed errors per R16). The Assistant is a **separately deployable** component; whether it actually runs out-of-process on day one is Q10, not a given (C1 as corrected). What is non-negotiable is its *call surface*: read-only Context Sets + the suggestion queue. Running it in-process behind that surface is defensible for a first slice (R20) provided the boundary is enforced in code and the egress/trust decision (Q10, A7) is taken deliberately. |
| Data / storage | One relational DB. Objects: a shared `object` table (id, type, owner, created_by, timestamps, **nullable** `home_project` for the Inbox — R21) + per-type detail tables; `contributor` table; per-object `activity` records (R22). Because object type is mutable (R21), either keep type-specific fields in a flexible column on `object` or make re-typing an explicit, cheap detail-row move — this is a real design choice the physical model must answer, and it is the one dimension where a document-shaped store is genuinely more natural. Relationships: one `relationship` table (`from`, `to`, `verb`, `origin ∈ {explicit,structural,inferred}`, `confidence_state`, `author`, `created_at`, **`visibility` (R9a)**, `signals` JSON). Candidate churn isolated in `relationship_suggestion` and `relationship_suppression` tables → C2 satisfied without a second datastore. `usage_signal` table (per-user). `audit_log` table (R15). Telemetry/eval events (R23) captured as their own append-only stream, scoped so they never widen R9. |
| Search / retrieval | Full-text index for the content signal + a distinctive-term/entity index (derived table). Vector similarity via the DB's vector capability/extension — one store, one backup, embedding row transactionally consistent with its object text. Structural traversal (shared Project, `supersedes` chains, Idea→Task lineage, 1–2-hop indirect context) via recursive CTEs, hop-capped per R6. |
| Context / relationship model | Relationships are edge rows. Context Set assembly = a parametrised query plan per purpose (Resume/Decision-recall/Handoff) that unions the signal queries, then an **app-side ranker** applies versioned weights (C6) and emits a factor breakdown (R7). Reproducible because every input is a row. |
| AI integration | Assistant service behind an `LLMProvider` interface (no provider chosen, C7). It calls only `ContextEngine.getContextSet(purpose,target,scope,user)` and posts to the suggestion queue (R12). Engine unavailability → typed error → retry-offer (R16). |
| AuthN / AuthZ | Identity/Sharing module. Authorization = a single visibility predicate composed into every engine query by one query-builder (C3, EC2). `project_share` rows drive it. Both-endpoints-visible enforced in the relationship query's join, **plus the relationship's own visibility attribute (R9a)** — endpoint visibility alone would wrongly expose one teammate's private links between two shared objects. Side-channel-safe: counts/rankings are computed only over the already-filtered row set. |
| Background / async work | Signal recomputation, embedding generation, and candidate scoring are batch workloads that must not contend with the interactive assembly path. In A they run as a worker process against the same DB (optionally a read replica) — an internal boundary, not a new architecture. Inference scheduling itself remains open (Q7). |
| Individual + small-team | Solo = one owner, zero `project_share` rows, sharing module dormant (C9/R17). Team = insert shares. No new infrastructure. |
| GitHub / integration readiness | Phase-4 = new `object.type` values + an ingestion worker writing objects and feeding `relationship_suggestion` / `usage_signal`. Purely additive (R19/C8). |
| Local dev | One DB container + app + an Assistant stub. Single `docker compose up`; deterministic seed data. Best time-to-first-slice (R20/EC7). |
| Testing | Rankers and scorers are pure functions (unit). Assembly: DB-fixture integration tests. Engine API: contract tests reused when/if extracted. Privacy: property-based tests asserting no object crosses a scope boundary over generated share graphs. Assistant: fake engine + fake LLM. Determinism (R7) makes golden-file ranking tests viable. |
| Deployment / ops | 2 services + 1 DB. One backup/restore path. Standard migrations. |
| Scalability | Vertical + read replicas; tenant partitioning later. Comfortably covers A1; not built for 10⁶-user social scale (not required). |
| Performance | Hot path (assembly) is one DB round trip (CTE + vector + full-text) then in-proc ranking; assembled Sets cacheable with write-triggered invalidation. Meets A3. Weak at deep graph analytics — out of MVP scope. |
| Security / privacy | Single store → one encryption-at-rest config, one audit log, one place to reason about leakage. Row-level scoping is inspectable. Strongest EC2/EC5 story. |
| Data ownership / portability | One logical dump = complete export (EC14 ✓✓). Self-host = 1 DB + 2 services (EC15 strong). |
| Development velocity | Highest: familiar model, transactional writes, no cross-store sync code. |
| Operational complexity | Lowest (EC12 ✓✓). |
| Cost | Lowest (EC13 ✓✓). |
| Major risks | (a) Vector-extension performance ceiling at growth → mitigated by the `RetrievalProvider` seam (swap to a dedicated vector store without schema change). (b) Modular boundary erosion → mitigated by the pre-defined engine API + architecture tests. (c) Recursive-CTE cost if indirect hops grew past 2 → bounded by R6 caps; precompute if needed. (d) Highly-connected object hot rows → indexes + per-layer caps. (e) A1 wrong → extraction path (below). |
| Migration / exit | Engine already has a service-grade API → lift to its own service. Embeddings behind an interface → move to a dedicated vector DB. Explicit-relationship rows map 1:1 to graph edges → a later graph projection is a straight ETL. Canonical relational export bounds lock-in. |

---

## 8. Architecture Alternative B — Graph-core (native graph database as system of record)

**Shape.** An application around a native graph database that stores objects as nodes and relationships as edges. The Context Engine leans on graph pattern-matching and traversal.

**Fair-comparison correction.** Two things were previously bundled into B that are *not* intrinsic to a graph core, and unbundling them removes an unequal comparison against A:
1. **Service decomposition.** B was written as "Workspace service + Engine service + Assistant service" while A was written as a modular monolith. Decomposition is orthogonal to the storage choice: a **graph-core modular monolith** (one app + one graph DB) is equally available, and it recovers most of B's penalty on EC6/EC7/EC12/EC13. The comparison below assumes that fairer variant.
2. **External indexes.** Whether full-text and vector search must live outside the graph is **product-dependent, not categorical** — some graph databases ship native full-text and vector indexing; others do not. Treating "separate vector DB from day one" as a property of graph-core overstated its polyglot cost. It is therefore a *product-selection risk* under B (the choice may or may not force polyglot), not a certainty.

What survives unbundling — and remains B's real cost — is the visibility-predicate spread across traversals (EC2), the export/self-host story if indexes do end up external (EC14/EC15, themselves assumption-derived), ecosystem familiarity, and query-language lock-in (EC17).

| Dimension | Analysis |
|---|---|
| Application architecture | Assessed in its fairest form: a modular monolith over a graph DB (Context Engine thin — much logic is graph queries), with the Assistant behind the same call surface as in A. |
| Data / storage | Graph DB is SoT. Nodes = objects; edges = relationships with properties (`verb`, `origin`, `confidence_state`, `author`, `ts`, `signals`). Authoritative vs inferred = edge label/property; suppressions = edges or a side store. Provenance = properties. **C2 concern:** authoritative and candidate edges live in the same store and are separated only by convention/labels, not by lifecycle isolation. |
| Search / retrieval | Full-text and vector support **vary by product**: some graph engines index both natively (no polyglot cost), others force external stores and cross-store hydration on the hot path. This is a product-selection risk, not a fixed property. Candidate generation = graph patterns + similarity lookup. Indirect-context traversal is native and cheap. |
| Context / relationship model | Best *conceptual* fit for "typed directional relationships" and multi-hop assembly. But ranking still must be app-side for explainability/reproducibility (R7/C6) — graph-native scores (e.g. centrality) are out of MVP scope and less explainable — so the graph's headline advantage is only partly used. |
| AI integration | Same separate untrusted service + suggestion queue. |
| AuthN / AuthZ | Visibility must be applied inside **every** traversal. Enforcing both-endpoints-visible and no-side-channel across arbitrary traversal shapes is harder to centralise than a single SQL predicate → **weaker EC2**, more surface for a privacy defect. |
| Individual + small-team | Conceptually identical model; fine. |
| GitHub / integration readiness | Excellent — external activity is just more nodes/edges (EC9 ✓✓). |
| Local dev | Graph DB + app (plus external search/vector only if the product lacks them). Main drag is query-language fluency, not container count; first slice slower than A, faster than previously stated. |
| Testing | Traversal queries are harder to fixture and exhaustively assert; privacy property tests must cover many traversal shapes. Ranking still testable. Net EC16 weaker than A. |
| Deployment / ops | Specialised operational knowledge (backup/restore, HA, upgrades less commoditised); **if** the product forces external search/vector, add cross-store reconciliation with no cross-store transactions. Worse than A, better than C. |
| Scalability | Graph DBs read-scale well; clustering/write-scaling can be licensed/costly. Exceeds A1 need (EC10 — headroom not required). |
| Performance | Multi-hop traversal is fast, but the MVP caps at ~2 hops (P2.2 §6), so the advantage is largely dormant; cross-store hydration adds latency on the hot path. |
| Security / privacy | Multiple stores → multiple encryption/audit configurations; unified audit log needs aggregation. EC2/EC5 weaker than A. |
| Data ownership / portability | Export spans graph dump + search + vector; self-host runs three systems (EC14/EC15 weaker). |
| Development velocity | Slower initially: fewer fluent developers, more moving parts, consistency code. |
| Operational complexity | High. |
| Cost | Higher (managed graph + vector + search, or heavier self-host ops). |
| Major risks | (a) Query-language / vendor lock-in (Cypher vs GQL vs proprietary). (b) Privacy predicate spread across traversals. (c) Polyglot consistency bugs. (d) Hop-cap makes the core advantage marginal for the MVP. (e) Hiring/knowledge risk. |
| Migration / exit | Hardest: data model and query language are product-specific; moving off graph is a Context Engine rewrite. |

---

## 9. Architecture Alternative C — Hybrid: relational source-of-truth + derived graph projection + dedicated vector/search indexes (event-driven)

**Shape.** Relational DB is authoritative for objects, explicit relationships, provenance, sharing, and audit. A **derived** graph projection (rebuilt from an event stream) serves traversal and inferred-candidate maintenance. A dedicated vector DB and a dedicated search engine serve semantic and full-text signals. The Context Engine owns the projection + a separate candidate store. Assistant service as in A. Components communicate over an event bus.

| Dimension | Analysis |
|---|---|
| Application architecture | Workspace service (owns relational SoT) + Context Engine service (owns projection, candidate store, index fan-out) + Assistant service + event bus + projection workers. |
| Data / storage | Relational SoT for objects / explicit relationships / provenance / sharing / audit. Inferred candidates + suppressions + usage signals in the engine's **own** store → **cleanest C2**. Graph projection is disposable and rebuildable. Vector DB + search engine are best-of-breed. |
| Search / retrieval | Best-of-breed per concern; candidate generation fans out across search + vector + projection; ranking app-side (R7). |
| Context / relationship model | Strong: SoT integrity from relational, traversal speed from the projection, semantic recall from vectors. But a just-created explicit relationship not yet in the projection would be missed → contradicts R3's "immediately honored" unless explicit relationships are always read from the SoT directly and the projection is used only for inferred/traversal — extra code and a subtle correctness rule. |
| AI integration | Same separate untrusted service + suggestion queue. |
| AuthN / AuthZ | Authoritative permissions in relational, but the projection and both indexes must respect visibility too. Mitigation: the engine resolves final visibility against the relational SoT before emitting any Set (one checkpoint) — restores EC2 at a latency cost and with more moving parts to keep aligned. |
| Individual + small-team | Same model — but running an event bus + projection workers + three datastores to open a solo notes app is disproportionate (EC6/EC18 poor). |
| GitHub / integration readiness | Excellent — new event sources, projection and indexes absorb them (EC9 ✓✓). |
| Local dev | Heaviest stack; the event pipeline must be running before anything appears; slowest onboarding; directly in tension with R20/EC7. |
| Testing | Highest burden: eventual-consistency scenarios, projection-rebuild tests, cross-store privacy tests, event-replay tests. EC16 weakest. |
| Deployment / ops | Highest: bus, workers, 3+ datastores, backfill/rebuild tooling, projection-lag monitoring (interacts with the staleness semantics of R18). EC12 weakest. |
| Scalability | Highest ceiling; each concern scales independently — **far beyond A1**. |
| Performance | Hot path can be fastest once warm, but projection lag adds a consistency/staleness dimension the product must reason about. |
| Security / privacy | Largest attack surface; strongest *possible* isolation but the most configuration to keep aligned; unified audit needs aggregation. |
| Data ownership / portability | Relational SoT gives a clean canonical export (EC14 good); full *system* reproduction is complex (EC15 poor). |
| Development velocity | Slowest to first slice; fastest to add new signal types much later. |
| Operational complexity | Highest. |
| Cost | Highest. |
| Major risks | (a) Premature complexity vs actual scale (A1) — violates "avoid unnecessary complexity" and the "no large invisible infrastructure first" value (R20/EC18). (b) Projection lag vs R3 immediacy. (c) Eventual-consistency defects. (d) Privacy alignment across four stores. (e) Long runway before visible product. |
| Migration / exit | Data export decent (relational SoT); operational/knowledge exit poor. |

---

## 10. Comparison Matrix

`++` strong / `+` adequate / `−` weak / `−−` poor, judged against the *documented* requirements and A1 scale (headroom beyond need is **not** rewarded).

| Criterion (weight) | A: Relational-core | B: Graph-core | C: Hybrid event-driven |
|---|:--:|:--:|:--:|
| EC1 Context Engine conceptual fit (H) | + | ++ | ++ |
| EC2 Privacy enforceability / centralised (H) | ++ | − | + (via SoT re-check) |
| EC3 Authoritative vs inferred separation (H) | + (separate tables) | − (label-only) | ++ (separate store) |
| EC4 Reproducible + explainable ranking (H) | ++ | + | + |
| EC5 Provenance + unified audit (H) | ++ | + | + |
| EC6 Individual-first simplicity (H) | ++ | ++ ‡ | −− |
| EC7 Local dev / time-to-first-slice (H) | ++ | + ‡ | −− |
| EC8 Ongoing dev velocity (M) | ++ | − | − |
| EC9 Phase-4 extensibility (M) | + | ++ | ++ |
| EC10 Scalability headroom vs A1 need (M) | + (sufficient) | + (excess) | + (excess) |
| EC11 Hot-path performance, MVP (M) | + | + | + |
| EC12 Operational complexity (H, lower better) | ++ | + ‡ | −− |
| EC13 Cost at target scale (M) | ++ | + ‡ | −− |
| EC14 Data export / canonical dump (H, assumption-derived) | ++ | + ‡ | + |
| EC15 Self-host feasibility (M, assumption-derived) | ++ | + ‡ | − |
| EC16 Testing tractability (H) | ++ | + | − |
| EC17 Migration flexibility later (M) | + | −− | + |
| EC18 "Avoid unnecessary complexity" (H) | ++ | − | −− |
| EC19 Eval-telemetry + background-work isolation (M) | + | + | ++ |

**‡ B is scored in its fairest form** (modular monolith; graph product with native full-text + vector indexing) per §8's fair-comparison correction. If the selected graph product forces external search/vector stores, **B drops one grade on each of EC7, EC12, EC13, EC14, EC15**. Its EC6 (individual-first), EC7, EC12 and EC13 scores above are the corrected, un-bundled ones.

**Reading the matrix:** A dominates on every high-weight criterion except EC1 and EC3, where it is merely *adequate*. B — even after the fair-comparison correction, which materially improves its simplicity and cost scores — still leads only on EC1 and EC9, both partly nullified for the MVP by the 2-hop cap (R6) and the exclusion of learned/graph-native scoring (P2.2 §12), while regressing on EC2 (privacy predicate spread across traversals), EC3, EC17 (query-language lock-in) and EC18. C leads on EC3, EC9 and EC19 but is the weakest option on six high-weight criteria, and its complexity is explicitly counter to R20/EC18 at A1 scale.

**Sensitivity check.** EC14/EC15 are assumption-derived (A2, see §5 sourcing note). Removing them entirely from the matrix does **not** change the ordering: A still leads on EC2, EC4, EC5, EC6, EC7, EC12, EC16 and EC18 — all high-weight, all requirement-traced. The recommendation therefore does not rest on the unsourced portability criteria.

---

## 11. Trade-off Analysis

- **Conceptual fit vs enforceability.** B/C model relationships more "naturally", but the two properties the documents treat as hard invariants — centralised privacy filtering with no side channel (R9/C3) and reproducible, explainable ranking (R7/C6) — are *easier to guarantee and test* when all inputs are rows behind one query path (A). A graph's expressiveness is not the bottleneck; the MVP forbids the features that would exploit it.
- **Scale headroom vs delivery.** C buys independent scaling the product does not yet need (A1) at the cost of the thing the project explicitly values — visible vertical slices and low invisible infrastructure (R20). Headroom that delays the first usable slice is a net negative here.
- **Store separation.** C2 wants authoritative and inferred stores separable. C does this best (distinct store); A does it well enough (distinct tables + distinct write paths + independent recompute/retention); B does it worst (same store, convention-only). A's approach is sufficient for the requirement as written.
- **Portability — weaker argument than it appeared.** No accepted document requires export, portability, data ownership, or self-hosting (§5 sourcing note); this is assumption A2 only. Under A2 a single relational SoT (A, and the SoT half of C) yields a cleaner canonical export than a polyglot SoT, and self-host is easiest for A — but the sensitivity check in §10 shows the ordering holds even if A2 is dropped, so this argument is **supporting, not load-bearing**.
- **Extraction path.** A is not a dead end: its pre-defined engine API, `RetrievalProvider`/`LLMProvider` seams, and edge-row model that maps 1:1 to graph edges mean the team can evolve toward B-style traversal or C-style projections **if and when** A1 proves wrong — without a rewrite. The reverse (starting at B/C and simplifying) is far harder.
- **Hidden coupling that would make A → C hard (the migration hazard §7 understated).** The claim that A evolves cleanly toward C is true of the *data* and false of the *code* unless three couplings are addressed from the start: **(i) strong-consistency assumptions** — A's assembly reads objects, edges, and indexes in one transaction and keeps embeddings transactionally consistent with object text; every such call site becomes a correctness question the day a projection or index goes eventually-consistent, which is precisely C's model. **(ii) Cross-module transactional writes** — if a capture writes the object, its `belongs to` edge, an activity record, and a candidate row in one DB transaction, splitting the Engine out later needs an outbox/saga rework; adopting a transactional-outbox seam on day one is cheap and removes the hazard. **(iii) Schema-less provenance** — a free-form `signals` JSON blob is convenient now but resists the queryable, versioned explainability C6 requires and complicates any later projection; the fields that drive ranking or explanation should be typed. Mitigation for all three is inexpensive *if decided in P2.5*, expensive afterwards.
- **Where C's ideas still apply.** Even under A, four C-flavoured decisions are worth keeping: a *separate* candidate/suggestion store boundary (already in A's design), retrieval behind a provider seam, a transactional-outbox / typed-event seam for write→cache-invalidation and future ingestion, and an isolated worker for signal recomputation (EC19).

---

## 12. Recommended Candidate (for P2.5 to ratify or reject)

**Alternative A — relational-core modular monolith with a separate Assistant service and retrieval behind a provider seam — is the strongest candidate**, because it satisfies every hard constraint (C1–C9) and every high-weight criterion at least adequately, with the lowest operational complexity, the best local-dev and portability story, and a non-destructive path to B/C-style components later. B's advantages are dormant under the MVP Context Engine scope; C is over-built for A1 and works against the project's stated delivery values.

**Why "lowest complexity" is not the argument.** A is not preferred merely because it is simplest. It is preferred because the two properties the accepted documents treat as *hard invariants* are strongest under it: **(1) centralised, auditable privacy enforcement** — R9 plus R9a's relationship-level visibility and the no-side-channel rule are enforceable in one composable predicate on one query path, whereas B must reapply them inside every traversal shape and C must reconcile them across four stores; and **(2) reproducible, explainable ranking (R7/C6)** — trivially satisfied when every ranking input is a row in one transactionally consistent store, and progressively harder as inputs spread across eventually-consistent components. Operational simplicity, cost and local-dev speed are corroborating, not decisive; strip them out and A still leads on EC2, EC4, EC5 and EC16.

**Where A is genuinely weaker.** EC1 (conceptual fit) and EC3 (store separation) — B and C model relationships and candidate isolation more naturally. A's answers (edge rows; separate candidate tables with independent write paths and retention) are *sufficient for the requirements as written*, not superior. P2.5 should accept this consciously.

This is a recommendation, not a decision. It is contingent on P2.5 confirming A2 (deployment/self-host — note EC14/EC15 are assumption-derived), A4 (embeddings in/out of MVP), and Q10 (Assistant trust zone, which C1 no longer pre-decides). Per the corrected A1 analysis, **object-count scale alone does not flip the recommendation**; the trigger for C's shape is several concerns needing simultaneous independent scaling plus a team large enough to operate them.

---

## 13. Rejected / Less-preferred Approaches

| Approach | Why not (traced to requirements) |
|---|---|
| **Document / KV / wide-column core** as SoT | Context Set assembly is inherently multi-entity and relationship-centric (R2, R5, R6); ad-hoc join-like assembly and the centralised visibility predicate (R9/C3) are awkward and error-prone without relational joins or graph edges. No compensating advantage at A1 scale. |
| **Local-first / CRDT store as the core** | Attractive for portability (assumption A2 — not a documented requirement) and solo use, but team sharing (R10), server-side per-viewer context computation and no-side-channel enforcement (R9), embeddings, and the audit log (R15) all require an authoritative server anyway. Revisit as an *optional sync/offline layer* on top of A, not as the core. |
| **RAG-only / "LLM as the context store"** (no explicit relationship store; retrieve-and-reason over embeddings) | Directly violates C1, C2, R3, R11, R12 — the Assistant would become the de facto system of record for how context connects, ranking would be neither reproducible nor explainable (R7/C6), and privacy could not be centrally enforced (C3). |
| **Full microservices from day one** (separate Workspace, Relationship, Signal, Assembly, Ranking, Identity services) | No requirement calls for independent scaling of these at A1; multiplies operational complexity and cross-service privacy/consistency surface against R20/EC18. The one boundary the documents *do* mandate — Assistant vs Engine (C1) — is honored in A. |

---

## 14. Risks (cross-cutting, independent of the chosen alternative)

| Risk | Impact | Mitigation |
|---|---|---|
| Embedding/vector performance or cost ceiling | Degrades the content signal (R5) or raises cost | Retrieval behind a provider seam; start with full-text + distinctive-term index, add vectors as a swappable component |
| External-AI egress of sensitive content (A7) | Privacy exposure via the Assistant's LLM calls | Define the Assistant trust zone, egress controls, and a redaction/consent point in P2.5; keep the Engine (which holds all context) with no external egress |
| Privacy filtering defect (side channel via counts/rankings) | Leak of invisible object existence (R9) | Single visibility query path (favoured by A); property-based privacy test suite as a release gate |
| Ranking non-reproducibility (unversioned weights, hidden state) | Violates R7/C6; erodes user trust in HITL corrections | Versioned weight constants (A8); ranker emits a factor trace; golden-file tests |
| "Work session" window undefined (A5) | Temporal signal miscalibrated | P2.5/impl decision; make it configurable |
| Context Set cache staleness vs R3 immediacy / R18 staleness semantics | Stale or missing just-added context | Explicit relationships read from SoT, not cache; write-triggered invalidation; surface "last updated N ago" |
| Scale assumption A1 wrong | Chosen shape under-provisioned | A's extraction path (engine API, provider seams, edge-row model); revisit at defined object-count thresholds |
| Modular boundary erosion (A) | Future extraction becomes costly | Architecture/dependency tests enforcing the engine API boundary |
| **Relationship visibility modelled as endpoint-derived only** (R9a) | A teammate's private link between two shared objects leaks to everyone with access to both endpoints — a real privacy defect the "both endpoints visible" rule does not catch | Visibility is an attribute of the relationship as well as its endpoints; cover it explicitly in the privacy property tests and the release gate |
| **Prompt injection via workspace content** | Shared Notes/Decisions authored by a teammate (and, from Phase 4, ingested GitHub/chat text) reach the Assistant's model as content; a crafted payload could attempt to steer proposals | Already bounded by R12/C4 (output is data, proposals are inert until confirmed, write surface is only the suggestion queue) — but the boundary must be tested, and Phase-4 ingestion widens the untrusted-input surface |
| **Background inference contending with the interactive path** (EC19) | Recompute/embedding batches degrade Resume-view latency (A3) | Isolate signal recomputation to a worker (and optionally a read replica) from day one; it is an internal boundary in A, not a new architecture |
| **Evaluation telemetry violating R9** | Capturing the metrics R23 requires could copy private content into a less-guarded store | Capture identifiers, states and outcomes rather than content; scope telemetry to the same visibility rules; decide placement in P2.5 (Q9/Q14) |

---

## 15. Migration / Exit Considerations

- **Keep the Context Engine behind a stable, typed API** (including the error contract for R16) from the first commit, whether or not it is a separate process — this is the single most important reversibility investment.
- **Isolate retrieval** (`RetrievalProvider`) and **model access** (`LLMProvider`) behind interfaces; no provider name in core code (C7).
- **Store explicit relationships as edge rows that map 1:1 to graph edges** so a later graph projection or migration to B is an ETL, not a redesign.
- **Guarantee a canonical export**: a documented, versioned dump of objects + relationships + provenance + audit in an open format — the ultimate exit hatch. (Motivated by assumption A2, which P2.5 must confirm; no accepted document requires it today.)
- **Avoid datastore-proprietary features** except the vector capability, which is already behind the seam.
- **Version the ranking weights and the Context Set schema** so behaviour is reproducible across upgrades and a consumer (the Assistant) can detect changes.

---

## 16. Open Architectural Questions (for P2.5)

1. **Confirm A1 scale.** Target maximum team size and expected workspace object counts / growth curve. *Reclassified:* an assumption to **validate**, not a gate — per §4/A1, object count alone does not flip the recommendation; the flip trigger is simultaneous independent scaling of several concerns plus operating-team size.
2. **Embeddings in the MVP?** Is the content/semantic signal (R5) delivered with vectors from day one, or with full-text + distinctive-term matching first? If vectors: hosted or self-hosted, and is external egress of object text acceptable (A7)?
3. **Vector search placement.** In-database vector capability vs a dedicated vector store — what performance envelope is acceptable at A1, and what is the trigger to split it out?
4. **Deployment & tenancy.** Single-tenant vs multi-tenant; is self-host a v1 requirement? (Shapes A2 and weights EC15.)
5. **Visibility enforcement pattern.** Mandate a single query-builder / single predicate path? Where are **both** checks implemented — "both endpoints visible" *and* the relationship's own visibility (R9a, private links on shared objects) — and how are they tested as a release gate?
6. **Context Set assembly mode.** Assemble-on-read vs precompute/cache; invalidation triggers; maximum acceptable staleness (interacts with R18).
7. **Inference scheduling.** Event-driven on capture/edit vs periodic batch vs hybrid (P2.2 §16); latency budget for a suggestion to appear.
8. **Ranking weights.** Where do the versioned constants live, how are they changed, and how is a change surfaced to the Assistant (C6)?
9. **Audit log placement & retention.** Same database as the SoT vs separate; retention period; export inclusion.
10. **Assistant trust zone.** Same network/security zone as the Engine or isolated; egress policy for external LLM calls given A7; where redaction/consent happens.
11. **Conversation store.** Where conversation state lives (not the workspace SoT, R14) — separate schema, separate store, retention, cross-device sync.
12. **"Work session" window.** Definition and default value for the temporal signal (A5).
13. **Evaluation telemetry (R23).** What is captured, where it lives, and how it is kept inside R9 (identifiers/outcomes rather than content). Without this the quality criteria both accepted designs define cannot be measured.
14. **Background/async execution model (EC19).** Where signal recomputation, embedding generation and candidate scoring run, and how they are isolated from the interactive assembly path (A3). Related to but distinct from Q7 (scheduling policy).
15. **Object type mutability and the Inbox (R21).** How re-typing and re-homing are modelled physically so that "refinement without penalty" stays a cheap transaction.
16. **Consistency and transaction boundaries.** Are Workspace and Engine writes allowed to share a transaction, or is a transactional-outbox seam adopted from day one (§11 hidden coupling)? This determines how expensive an A → C evolution ever becomes.

### Classification of these questions

- **Must be answered in P2.5 (they change the architecture decision or its enforceable invariants):** Q2 embeddings-in-MVP + external egress (A4/A7), Q4 deployment & tenancy, Q5 visibility enforcement incl. R9a, Q10 Assistant trust zone (now genuinely open after the C1 correction), Q16 transaction boundaries.
- **Assumptions to validate, not gates:** Q1 scale, Q3 vector placement, Q12 work-session window.
- **Answer in P2.5 as concrete design decisions if Alternative A is ratified:** Q6 assembly mode, Q8 ranking-weight governance, Q9 audit placement, Q11 conversation store, Q13 telemetry, Q14 background execution, Q15 type mutability.
- **Safely deferred to implementation planning:** Q7 inference scheduling policy (once Q14's execution model is fixed), and every product-level choice already listed as deferred in §17.

---

## 17. Inputs Required for P2.5

P2.5 (Architecture Decision) cannot be completed without:

- **Product/business inputs:** deployment & tenancy model + whether self-host is v1, and whether export/portability is a real product goal at all — **A2 is currently an unsourced assumption carrying two high-weight criteria** (§5 sourcing note); any compliance regime that changes A7; a budget ceiling for managed services (EC13); the relative priority of *delivery velocity* vs *scalability headroom* (re-weights EC10 vs EC7/EC12/EC18). A1 scale is validated here, not decided.
- **Technical inputs:** decision on embeddings-in-MVP and acceptable external-AI egress (A4/A7); acceptable Context Set assembly latency confirmed or revised (A3); a decision on assemble-on-read vs precompute (Q6); transaction-boundary policy (Q16); the "work session" window value (A5/Q12).
- **From this document:** ratify or reject Alternative A as the base shape; if ratified, close the "must answer" and "concrete design decision" questions in §16's classification — in particular Q5 (including R9a), Q10, Q13, Q14 and Q16, none of which P2.4 pre-decides; if rejected, state which criterion re-weighting drove the change and select B (in its corrected, fairly-scored form), C, or a stated variant.
- **Not required from P2.5, still deferred:** specific frontend framework, backend language/framework, database product, vector database product, AI provider/model, cloud/hosting platform, authentication provider — these remain open until implementation planning demonstrates a specific choice is warranted.

---

## 18. Requirement / Constraint / Assumption / Recommendation / Unresolved — index

- **Requirements:** R1–R23 incl. R9a (§2) — derived from accepted P1.1–P2.3; not created here.
- **Constraints:** C1–C9 (§3) — shape constraints implied by the accepted documents. C1 is a **logical** boundary; deployment topology is an open question, not a constraint.
- **Assumptions:** A1–A8 (§4) — introduced by this investigation; each flagged for P2.5 confirmation. A2 in particular carries two high-weight criteria (EC14/EC15) that no accepted document requires.
- **Recommendations:** Alternative A as strongest candidate (§12), argued from privacy enforceability and ranking reproducibility rather than from simplicity; reversibility investments (§15); adopt four C-flavoured seams inside A (§11). All contingent, none binding.
- **Unresolved:** Open Architectural Questions 1–16 with their must-answer / validate / defer classification (§16) and the P2.5 inputs (§17). P2.4 deliberately does not resolve these.

---

## Consistency with prior milestones

No contradictions with P1.1, P1.2, P2.1, P2.2, or P2.3. This document adds no product scope, changes no object model, and prescribes no technology; it evaluates architecture shapes against requirements those documents already fixed. Where P2.2 §16 / P2.3 §16 left decisions open, this investigation keeps them open and routes them to P2.5.

**Review corrections applied (P2.4 review, pre-acceptance).** (1) C1 previously stated that Engine and Assistant must be distinct *deployable/process* units — that over-read P2.2 §9 / P2.3 §2, which mandate a responsibility and call-surface split, and it pre-decided P2.3 §16's open "Assistant trust zone" question; C1 is now a logical boundary and the topology is Q10. (2) R9a added: relationship visibility is not purely endpoint-derived (P2.2 §11 private links on shared objects). (3) R21–R23 added: unfiled/mutable captures, per-object activity history, evaluation telemetry — all present in the accepted documents but previously unextracted. (4) EC14/EC15 were misattributed to "Blueprint §4"; no accepted document mentions portability, export, data ownership or self-hosting, so they are now marked assumption-derived (A2) and a sensitivity check confirms the ordering holds without them. (5) Alternative B was compared unfairly (more service decomposition than A, and a categorical claim that graph cores force external search/vector); it is now scored in its fairest form. (6) The A1 scale sensitivity was overstated — assembly is a bounded-neighborhood query, so object count alone does not flip the recommendation. (7) §11 now names the hidden couplings that would actually make an A → C evolution expensive.
